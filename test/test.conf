set -g default-terminal "tmux-256color"
set -g base-index 1
setw -g pane-base-index 1
# commands to unbind the default tmux prefix 
#
# kill window shortcut
bind-key k kill-window

run-shell "~/Projects/tmux-layouts-plugin/tmux-layouts.tmux"

bind-key S run-shell "bash ~/Projects/tmux-layouts-plugin/scripts/layout_state.sh"

bind-key L display-popup -E "echo 'Layouts:'; echo ''; ls -1 ~/Projects/tmux-layouts-plugin/layouts 2>/dev/null | sed 's/\.sh$//' | nl || echo 'No layouts yet'"

bind-key r source-file ~/Projects/tmux-layouts-plugin/test/test.conf \; display-message "Config reloaded!"

bind-key H display-popup -E "tmux show-hooks -g"

bind-key M show-messages

unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# commands to change window splitting 
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# pane navigation
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

set -g status-style bg=yellow,fg=black
set -g status-left "[TEST MODE] "
set -g status-right "Layouts: ~/Projects/tmux-layouts-plugin/layouts "
